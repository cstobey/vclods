: "${DIR_ERR_SHOULD_EXIT:=1}"
err_opt="$([ "$DIR_ERR_SHOULD_EXIT" -eq 1 ] && echo " || exit 1")"

cat
find "${DIR_EXT_DIR:=$INPUT_DIR}" ! -empty -xtype f -regextype posix-egrep -regex "$VCLOD_DIR_REGEX" -regex "^.*/${ext_opt:-${VCLOD_DIR_START:?}}[^/]*/[^/]+" ! -name config | sort \
  | awk -F'/' -v e="$err_opt" '{print "cat "$0" | vclod_operation "$(NF-1) e}' | . /dev/stdin \
  || { ret="$?" ; echo "$VCLOD_DIR_START Directory Failed ${VCLOD_DIR_CONTEXT:-}" >&2; exit "$ret"; }
#WRAP Run a subdirectory where the last directory name holds the .extension on how to process the files (the files may not have .extensions of their own).

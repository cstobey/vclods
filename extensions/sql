#ANY Run a SQL script with default connection (configured with VCLOD_SRC_)
: "${ext_opt:-BNArc if using mysql}"
CMD="$(literate_source "$SRC")"
[ "$?" -eq 0 ] || exit 1
[ ! -z "$CMD" ] || exit 1
envsubst | $CMD

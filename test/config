# Empty config file for vclod test
VCLOD_JOBS=100 # let's speed things up
VCLOD_LOCK_DIR=/dev/shm/
VCLOD_ERR_DIR=/dev/shm/
VCLOD_MYSQL_HOST=localhost
VCLOD_MYSQL_USER=myuser
VCLOD_MYSQL_PASSWORD=mypass
VCLOD_MYSQL_DB=information_schema
LOG_SQL_HOST=localhost
LOG_SQL_DB=test

LOG_BASE_DIR=./logs/
TEE_EXT_DIR=./files/
OUT_EXT_DIR=./files/
OPERATIONS_EMAIL=noop@noop.com

CURL_EXT_URL='http://localhost:9000/page_${CURL_EXT_LOOP_CNT}.json'
CURL_EXT_JQ_PAGE_PROG='.total'

. ${LOCAL_DIR}/secure_config  # so you can override the HOST, USER, and PASSWORD without accidentally putting it in version control

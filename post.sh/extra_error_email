: "${ELF:?Needs to be called after vclod_stdio}"
[ -z "${EXTRA_ERROR_EMAIL:=}" ] && return # must have an email to send to
[ ! -t 1 ] && mail -s "Script error digest on $(hostname)" "$EXTRA_ERROR_EMAIL" <"$ELF"
